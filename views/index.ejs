<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>Welcome to fruit app</title>
	<link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
	<h1>Here are some fruits</h1>
	
	<ul class="fruits">
		<% for(let i = 0; i < fruits.length; i++) { %>
			<li> 
				<a href="/fruits/<%= i %>"><%=fruits[i].name%></a>
				(<%= fruits[i].color %>)
				<% if(fruits[i].readyToEat) { %>
					(Edible! 🍉🍐🍏🍌🍑🍅🍍)
				<% } else { %>
					(<font color="red">NOT</font> edible 😪😳🤒💩😪)
				<% } %>
				<!-- as is, this button is not hitting the delete route.  why?  because HTML doesn't let browsers send DELETE/PATCH/PUT requests. we will install node module method-override -->
				<form action="/fruits/<%= i %>/?_method=DELETE" method="POST">
					<button type="submit">Delete</button>
				</form>	
			</li>
		<% } %>
	</ul>

	<a href="/fruits/new">ADD A FRUIT</a>

</body>
</html>